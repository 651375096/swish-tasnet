import torch


class Scaler:
    def __init__(self):
        return

    def compute_stats(self, dataset):
        # Loop over the dataset and compute the average power
        return

    def normalize(self, x):
        # Scale input to the given average power
        return new_x, norm_ratio
